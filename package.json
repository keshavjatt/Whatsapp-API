{
  "name": "whatsapp-api",
  "version": "2.0.0",
  "description": "Complete WhatsApp API with Dashboard",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "clear": "rm -rf ./.wwebjs_auth && npm start",
    "reset": "rm -rf node_modules package-lock.json && npm install",
    "test": "curl http://localhost:8000/status"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-fileupload": "^1.4.0",
    "express-validator": "^7.0.1",
    "qrcode": "^1.5.3",
    "socket.io": "^4.7.0",
    "whatsapp-web.js": "^1.23.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}